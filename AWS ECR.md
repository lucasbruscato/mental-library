ECR stands for Elastic Container Registry, it's function is:

1. To be a private Docker registry on AWS
2. It's where the Docker images are stored (the ones that can be run by [[AWS ECS]] or [[AWS Fargate]])
